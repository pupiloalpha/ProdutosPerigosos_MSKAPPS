<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProdutosPerigosos">
        <activity
            android:name=".TelaInicialApp"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".listas.ListaSubClasse" android:theme="@style/Theme.ProdutosPerigosos"/>
        <activity android:name=".info.DetalheProduto" android:exported="true" android:theme="@style/Theme.ProdutosPerigosos">
            <intent-filter>
                <action android:name="com.msk.produtosperigosos.PRODUTO" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>